info:
  name: Forget Password
  type: http
  seq: 8

http:
  method: POST
  url: "{{host}}/identity/forget-password"
  body:
    type: json
    data: |-
      {
        "email": "tarekmohamed1016@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
